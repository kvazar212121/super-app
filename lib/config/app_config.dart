/// Ilova konfiguratsiyasi.
class AppConfig {
  /// `false` — server siz lokal demo rejim (login, buyurtma, balans).
  /// Backend tayyor bo'lganda `true` qiling.
  static const bool useBackend = false;
}
